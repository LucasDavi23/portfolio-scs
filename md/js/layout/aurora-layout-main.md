### 🌇 Aurora — Líder do Layout

**Arquivo:** `aurora-layout-main.js`

**PT:**  
Aurora coordena tudo que pertence ao setor de Layout.  
Ela não executa tarefas individuais — quem faz isso são Luna, Navi, Aura, Iris e Nami — mas Aurora garante que **cada uma delas seja chamada na ordem correta**, mantendo o fluxo do layout limpo e estável.

Suas responsabilidades incluem:

- Inicializar a fundação do layout (Luna)
- Ativar a navegação mobile / submenu (Navi)
- Preparar efeitos de scroll (Aura)
- Habilitar o modal global de imagens (Iris)
- Iniciar o carrossel principal (Nami)
- Servir como ponto único de entrada para tudo relacionado ao layout

Aurora também facilita manutenção: basta consultar seu arquivo para entender rapidamente **o que o layout possui** e **quem é responsável por cada parte**.

Ela não cria UI própria, não altera DOM diretamente e não decide regras —  
ela apenas coordena, orquestra e garante que o layout inteiro funcione como uma sinfonia.

**EN:**  
Aurora coordinates everything that belongs to the Layout sector.  
She doesn’t execute individual tasks — Luna, Navi, Aura, Iris and Nami do that —  
but Aurora ensures each one of them is initialized in the correct order, keeping the layout clean and stable.

Her responsibilities include:

- Initializing layout foundation (Luna)
- Activating mobile navigation / submenu (Navi)
- Setting up scroll effects (Aura)
- Enabling the global image modal (Iris)
- Starting the main carousel (Nami)
- Serving as the single entry point for all layout-related logic

Aurora simplifies maintenance: by reading her file, you immediately understand  
**what the layout contains** and **who is responsible for each part**.

She does not create UI, does not manipulate the DOM directly and does not define rules —  
she simply coordinates, orchestrates and ensures the entire layout behaves like a symphony.
