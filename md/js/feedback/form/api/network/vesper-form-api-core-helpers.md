### 🧬 Vesper — Core de Rede (Form)

**Nível / Level:** Adulto / Adult

**PT:**  
Especialista na camada de rede do **Form**.

Responsável por:

- construir a **BASE URL**
- executar **POST JSON** com header `"text/plain"` (evitando preflight)
- realizar **parse seguro** das respostas
- **normalizar erros de rede**

Vesper não possui conhecimento sobre regras ou domínio do formulário.

**EN:**  
Specialist in the **Form network layer**.

Responsible for:

- building the **BASE URL**
- performing **JSON POST requests** with `"text/plain"` headers (avoiding preflight)
- safely parsing responses
- **normalizing network errors**

Vesper has no knowledge of form domain logic.
