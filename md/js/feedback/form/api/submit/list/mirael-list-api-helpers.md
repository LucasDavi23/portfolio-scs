### 🌸 Mirael — Feedback List API (Helpers)

**Nível / Level:** Jovem / Young

**PT:**  
Especialista na listagem de feedbacks do **Form**.

Responsabilidades:

- montar a **querystring do GAS** (`mode=list`)
- executar requisição **GET** para obter os dados
- retornar a resposta técnica para o fluxo de listagem

Mirael não envia payload, não possui conhecimento de UI
e não executa lógica de negócio.

**EN:**  
Specialist responsible for **Form feedback listing**.

Responsibilities:

- build the **GAS querystring** (`mode=list`)
- perform the **GET** request to retrieve feedback data
- return the technical response to the listing flow

Mirael sends no payload, has no knowledge of UI
and performs no business logic.
