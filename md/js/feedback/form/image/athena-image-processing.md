### 🧠 Athena — Image Processing Specialist

**Nível / Level:** Adulto / Adult

**PT:**  
Especialista em **processamento de imagem no formulário**.

Responsabilidades:

- validar arquivos de imagem
- converter e redimensionar imagens
- comprimir imagens (**WebP com fallback**)
- extrair **base64, mime e blob**
- gerar **nomes únicos para os arquivos**

Athena opera apenas no **processamento local (browser)**.

Não manipula UI, não executa chamadas de API/GAS
e não emite eventos.

**EN:**  
Specialist in **image processing for the form**.

Responsibilities:

- validate image files
- convert and resize images
- compress images (**WebP with fallback**)
- extract **base64, mime and blob**
- generate **unique filenames**

Athena operates only in **local processing (browser)**.

She does not manipulate UI, perform API/GAS calls,
or emit events.
